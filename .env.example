# Server
NODE_ENV=development
PORT=5174                                              # Backend API server port
SESSION_SECRET=replace-with-long-random-secret
CORS_ORIGIN=http://localhost:5173                     # Frontend dev server (Vite) - must match WEBSITE_URL

# Database
DATABASE_PROVIDER=sqlite
DATABASE_URL=file:./dev.db

# Email Configuration (REQUIRED for contact forms and partnership inquiries)
# For Gmail: Create an App Password at https://myaccount.google.com/apppasswords
# IMPORTANT: These are required - the app will fail to start without them
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-gmail-app-password-here
ADMIN_EMAIL=admin@example.com  # Optional: defaults to EMAIL_USER if not set

# Optional: Custom SMTP Configuration (overrides Gmail defaults)
# SMTP_SERVICE=gmail                    # Email service provider
# SMTP_HOST=smtp.example.com            # Custom SMTP host (overrides service)
# SMTP_PORT=587                          # SMTP port (587 for TLS, 465 for SSL)
# SMTP_SECURE=false                      # Use SSL (true for port 465, false for 587)

# Website Configuration
WEBSITE_URL=http://localhost:5173         # Frontend URL (where users access the site) - must match CORS_ORIGIN